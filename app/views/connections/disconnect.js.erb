const element = document.querySelector('#<%=@button%>-<%=@user.id%>-btn');
// console.log(element);

element.innerText = 'Connect with me';
element.classList.add('btn-primary');
element.classList.remove('btn-danger');
element.classList.add('disabled')